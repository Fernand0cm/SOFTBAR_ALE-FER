{
  "name": "softbar-firestore-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Pruebas de las reglas de seguridad de Firestore de SOFTBAR sobre el emulador.",
  "scripts": {
    "test": "firebase emulators:exec --only firestore \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.0",
    "firebase-tools": "^13.0.0"
  }
}
