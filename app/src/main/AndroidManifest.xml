<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".SoftbarApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TFG_SOFTBAR">

        <!-- Descarga el modulo del lector de codigos cuando hay WiFi -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="barcode_ui" />

        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.login.LoginActivity" />
        <activity android:name=".ui.home.HomeActivity" />
        <activity android:name=".ui.turno.TurnoActivity" />
        <activity android:name=".ui.mesas.MesasActivity" />
        <activity android:name=".ui.comanda.ComandaActivity" />
        <activity android:name=".ui.barra.BarraActivity" />
        <activity android:name=".ui.modificadores.ModificadoresActivity" />
        <activity android:name=".ui.cobro.CobroActivity" />
        <activity android:name=".ui.ticket.TicketActivity" />
        <activity android:name=".ui.caja.CajaActivity" />
        <activity android:name=".ui.informes.InformesActivity" />
        <activity android:name=".ui.stock.StockActivity" />
        <activity android:name=".ui.historial.HistorialActivity" />
        <activity android:name=".ui.config.ConfigActivity" />
    </application>

</manifest>
